@startuml
title PDFWriter Class Diagram

package "writer" {
  class PDFWriter {
    - {static} writer : PDFWriter
    + getInstance() : PDFWriter
    + writeQuoteToPDF(quote : Quote, companyName : CompanyName, filename : String) : void
    - addSummaryRow(table : PdfPTable, label : String, amount : double, columnSpan : int) : void
  }
}

package "data" {
  class CompanyName
  class Quote
  class Item
}

package "external libraries" {
  class Document
  class PdfWriter
  class PdfPTable
  class PdfPCell
  class Font
  class Phrase
  class Paragraph
  class Color
}

' Relationships
PDFWriter --> Quote : reads from
PDFWriter --> CompanyName : reads from
PDFWriter --> Item : iterates over
PDFWriter --> Document : creates
PDFWriter --> PdfWriter : writes to
PDFWriter --> PdfPTable : formats table
PDFWriter --> PdfPCell : formats cells
PDFWriter --> Font : styles text
PDFWriter --> Phrase : builds phrases
PDFWriter --> Paragraph : builds paragraphs
PDFWriter --> Color : sets background

@enduml
